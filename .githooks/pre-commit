#!/bin/sh

# Pre-commit hook to automatically bump patch version
echo "🔧 Auto-updating patch version..."

# Run npm version patch (without creating git tag)
npm version patch --no-git-tag-version

# Add the updated package.json and package-lock.json to staging
git add package.json package-lock.json

echo "✅ Patch version updated and staged"
